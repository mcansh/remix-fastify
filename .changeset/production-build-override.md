---
"@mcansh/react-router-fastify": patch
---

Use Vite's virtual React Router server build whenever a dev server is present, so production `build` overrides no longer disable hot-reloaded development builds.
